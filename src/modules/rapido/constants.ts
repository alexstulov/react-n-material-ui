const FIELD_SIZE: { first: number; second: number } = {
  first: 19,
  second: 2,
};
const FIELD_CHOICE_LIMIT: { first: number; second: number } = {
  first: 8,
  second: 1,
};

export { FIELD_SIZE, FIELD_CHOICE_LIMIT };
